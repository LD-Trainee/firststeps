{% extends 'base.html.twig' %}

{% block title %}Fritz{% endblock %}

{% block body %}
    <h1>Fritz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fritz.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fritz_index') }}">back to list</a>

    <a href="{{ path('fritz_edit', {'id': fritz.id}) }}">edit</a>

    {{ include('fritz/_delete_form.html.twig') }}
{% endblock %}
